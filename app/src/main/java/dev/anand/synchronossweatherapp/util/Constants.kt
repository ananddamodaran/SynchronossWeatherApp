package dev.anand.synchronossweatherapp.util

import dev.anand.synchronossweatherapp.BuildConfig

object Constants {
    const val DATABASE_NAME = "synchronoss-weather-db"
    const val REQUEST_CHECK_SETTINGS = 2

    const val GET_WEATHER="${BuildConfig.WEATHER_APP_URL}weather?units=metric"

}
